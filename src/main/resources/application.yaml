spring:
  application:
    name: todoapp

  datasource:
    url: jdbc:mysql://todoappdb.cpkkykeosln6.ap-south-1.rds.amazonaws.com:3306/todoapp?useSSL=false&allowPublicKeyRetrieval=true
    username: admin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8082

